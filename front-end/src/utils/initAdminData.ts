/**
 * Admin Data Initialization
 *
 * DEPRECATED: This file is no longer needed since we're using backend API.
 * All data (books, categories, authors, publishers, etc.) are fetched from the API.
 *
 * These functions are kept as stubs for backward compatibility.
 */

export const initAdminData = () => {
  console.log("initAdminData - Skipped (using backend API)");
  return Promise.resolve();
};

export function initializeAdminData() {
  console.log("initializeAdminData - Skipped (using backend API)");
  return;
}

export function updateCategoryBooksCount() {
  console.log("updateCategoryBooksCount - Skipped (using backend API)");
  return;
}
