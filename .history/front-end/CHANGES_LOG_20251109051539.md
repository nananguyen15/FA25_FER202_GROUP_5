# Danh sách các thay đổi đã thực hiện

## ✅ Hoàn thành

### 1. Fix Duplicate Keys Error trong Hero Slider
- **File**: `src/components/Home/HeroSlider.tsx`
- **Vấn đề**: Lỗi `Encountered two children with the same key, 2-84`
- **Giải pháp**: Thay key từ `${groupIndex}-${book.id}` → `hero-${groupIndex}-${bookIndex}-${book.id}` để đảm bảo unique

### 2. Fix Book Card Layout với Fixed Height
- **File**: `src/components/Home/BaseCard.tsx`
- **Thay đổi**:
  - Fixed height: 420px cho vertical card
  - Phân chia 3 phần rạch ròi:
    - **Part 1**: Ảnh (256px fixed height)
    - **Part 2**: Tên sách (line-clamp-2)
    - **Part 3**: Author + Price + Button cùng hàng (mt-auto để luôn ở dưới)
  - Dùng flexbox `justify-between` để content luôn đều

---

## 🔄 Đang thực hiện

### 3. Thêm hàng sách thứ 2 ở Homepage
**Kế hoạch**: Copy component `Somebooks`, rename thành `BestSellers` hoặc `NewArrivals`, fetch data khác

---

## 📋 Cần làm tiếp

### 4. Hiển thị FAQs từ faqData.ts
- Đọc file `src/data/faqData.ts`
- Render accordion UI trong `QuestionAndAnswer.tsx`

### 5. Fix Footer luôn dưới cùng
- Thêm CSS: `min-height: 100vh` cho main content
- Hoặc dùng flexbox sticky footer pattern

### 6. Dùng BookCard cho Browse Books
- Thay `ProductCard` trong `AllProducts.tsx` bằng `BookCard`
- Đảm bảo grid layout tương thích

### 7. Thêm phân trang
- Pre/Next arrows
- Số trang hiện tại
- Chọn trang
- Default 20 items/page
- Tính số trang: `Math.ceil(totalBooks / 20)`

### 8. Connect backend sort/filter APIs
**Backend APIs có sẵn**:
- `/api/books/active/sort-by-title`
- `/api/books/active/sort-by-price-desc`
- `/api/books/active/sort-by-price-asc`
- `/api/books/active/sort-by-oldest`
- `/api/books/active/sort-by-newest`
- `/api/books/active/search/{title}`

**Quyết định**: Connect backend APIs sẽ nhanh hơn vì:
- Không cần load toàn bộ data về client
- Backend đã optimize với database indexing
- Giảm memory usage ở client

### 9. Show active books trước, inactive sau
- Fetch `/books/active` trước
- Fetch `/books/inactive` sau
- Concat arrays: `[...activeBooks, ...inactiveBooks]`

### 10. Bổ sung FilterSidebar
**Cần thêm**:
- ✅ Sub-categories từ DB
- ✅ Filter sách available (stockQuantity > 0)
- ✅ Filter by Authors (fetch từ `/api/authors/active`)
- ✅ Fix Publishers từ DB (fetch từ `/api/publishers/active`)
- ✅ Collapse/Expand (+/-) cho sup-cate, author, publisher
- ❌ Bỏ phần "Type" filter

---

## 📝 Notes
- Hero slider: Hàng cuối đã canh giữa với `justify-center` (nếu có đủ 3 books trong group)
- Nếu vấn đề vẫn còn, có thể do CSS của individual book cards
